This works, but I know use Guard. It has more flexibility.

Watch the current folder recursively and run the tests with PHPUnit every time a file is changed.


It requires libnotify to watch the folder.
